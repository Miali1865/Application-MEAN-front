import { ServicesReparation } from './services-reparation';

describe('ServicesReparation', () => {
  it('should create an instance', () => {
    expect(new ServicesReparation()).toBeTruthy();
  });
});
